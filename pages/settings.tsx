import React from 'react';

import { MainWrapper } from '@components/wrapper';
import { SettingDetailPage } from '@components/Settings';

const Settings: React.FC = () => (
  <MainWrapper>
    <SettingDetailPage />
  </MainWrapper>
);

export default Settings;
